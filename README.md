## Rust Flake Template

This is a flake template for a rust develeopment environment using crane and rust-overlay.

Things to do:
- You need to change the package name in `Cargo.toml`
- You need to run `cargo generate-lockfile`
